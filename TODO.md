# CE 命中与火力分析器 TODO

目标：让当前手动计算器尽量贴近 Combat Extended 的真实普通直射流程，同时把较远、较复杂或暂时不重要的功能放到低优先级分类里。

## 当前优先级：手动计算器打磨

1. 重复射击转火角度。
   - 当前 `FasterRepeatShots` 已按“继续向同一方向射击”近似。
   - 如果后续需要估算频繁换目标，可加入“重复射击转向角度”输入，按 CE 的 `max(baseReduction, angleDiff / 45)` 修正预热缩短。

## 未来开发

这些功能有用，但不是当前手动计算器贴近普通 CE 射击流程的必要条件。

3. 武器配件。
   - 当前已支持读取选中 WeaponPlatform 的已安装配件，也支持在计算器里临时选择配件组合；切换品质/脚架时，临时武器副本会保留这些配件并应用 stat 替换、偏移、倍率。
   - 后续如有需要，可继续优化配件菜单的分组、搜索和槽位展示。

## 高级功能

这些功能优先级较低，因为它们需要实时地图状态、特殊 CE 弹丸 worker，或者超出普通直射的更大模型。

6. 掩体、目标碰撞盒裁剪、植物、建筑、屋顶、友军/身体阻挡。

7. 特殊弹丸类型。
   - 当前已添加特殊弹丸提示：载入瞬发、多弹丸、推进/变速、特殊轨迹、炮击属性、引信/空爆修正时会提示仍按普通直射近似。
   - 还没有真正分别建模这些弹道。
   - Instant。
   - Laser。
   - FlyOverhead。
   - Homing。
   - 燃料推进。
   - 多弹丸。

8. 连发中途重新瞄准和无射线 fallback。
   - 依赖当前目标、目标切换、射线、地图阻挡和 CE 设置。

9. 特殊伤害的护甲/抗性结算。
   - 当前护甲框只做普通锐甲与钝甲的近似过甲伤害。
   - 火焰、热、电击、毒气、EMP、特殊爆炸等 `isAmbientDamage` 或特殊 worker 暂不混入第一版。
   - 后续如果要做完整护甲 DPS，需要按 CE 的环境伤害百分比减免、抗性 stat、次级伤害触发概率和多层护甲分别建模。

## 已完成归档

这些项目对当前“手动普通直射模型”已经视为完成，只保留为背景记录。

- 复现普通 `Verb_LaunchProjectileCE.ShiftTarget` 的目标调整顺序：
  真实目标点、能见度圆形偏移、估计距离、预判偏移、枪口摆动、后坐力、机械散布。
- 射程误差已按 CE 风格影响估计距离和垂直/下坠补偿；测试覆盖了有预判偏移时对水平命中的影响。
- 武器摆动使用 CE 的连续 tick 模型：
  水平 `sway * sin((tick + shooterId) * 0.022)`；
  垂直 `0.25 * sway * sin((tick + shooterId) * 0.0165)`。
- 连发模型现在会共享首发估计距离、使用连续摆动时间、随发数增长后坐力、每发独立机械散布，并从 RPM 推导连发间隔。
- 已调查 `lastRecoilDeg`。当前 CE DLL 里它会累积/保存，但没有在 `Verb_LaunchProjectileCE` 中发现后续弹道读取，所以暂不建模。
- 普通弹道解算使用 CE 的低抛直射公式，并使用 `ProjectilePropertiesCE.GravityPerWidth = 9.8 / 5 * gravityFactor`。
- 当前用途下已匹配 CE 随机分布：
  能见度/圆形偏离/间瞄目标偏移使用 CE 风格 `GenRandInCircle`；
  机械散布使用 `Rand.InsideUnitCircle * spreadDegrees`。
- 手动输入下已统一使用 CE 风格派生误差：
  能见度误差、预判误差、射程误差和环境偏移。
- 已复现 CE `lightingCurve` 点：
  `(5,0.05)`、`(10,0.15)`、`(22,0.475)`、`(35,1)`、`(60,1.2)`、`(90,2)`。
- 已实现手动瞄准模式：
  瞄准射击、快速射击、压制射击。
- 有效枪口摆动由基础枪口摆动推导：
  瞄准射击应用瞄准精度/视野效率修正；
  快速射击和压制射击不应用瞄准减摆；
  射手被压制时基础摆动乘以 1.5。
- 已实现手动目标部位模式：
  躯干、头部、腿部。
- 压制射击和武器掌握低于 `2.45` 时使用躯干/默认瞄准高度。
- 已建模手动目标移动方向和预判距离。
- 公式参考输出已和蒙特卡洛真实射击采样分离。
- UI 已突出蒙特卡洛首发和连发至少一发命中率，CE 公式只作为参考。
- 已实现基础轴向诊断。
- 已添加更具体的结果提示：
  后坐力主要影响后续弹、水平/垂直限制判断、射程误差在垂直饱和时可能不改变总命中、无黑暗/天气/烟雾/移动时视野效率影响可能不明显等。
- 已添加分析模式开关，同一时间只运行一种额外分析，避免计算量爆炸。
- 已添加距离曲线，默认显示 10、20、30、40、50 格内不超过最大射程的点，并使用单独的较低样本数。
- 已添加连发逐发表格/条形图，复用主计算结果，不额外增加蒙特卡洛计算量。
- 已能从选中 pawn 的主武器或选中枪械读取武器数据，并可选择 CE 弹药：
  最大射程、弹速、弹丸重力、机械散布、基础枪口摆动、后坐力、视野效率、连发弹数、RPM；选中 pawn 时也会读取武器掌握、瞄准精度和 thingIDNumber。
- 已能从选中目标读取 CE 碰撞宽度/高度，自动填入目标宽度和目标高度。
- 已拆分武器与射手载入：
  武器/弹药负责武器参数，射手负责武器掌握、瞄准精度、枪口高度和摆动相位。
- 已接入武器品质：
  仅有品质组件的武器显示品质按钮，并按品质读取武器 stat。
- 已接入 CE 脚架：
  仅有脚架组件的武器显示脚架按钮，展开/未展开会影响额外射程、后坐力倍率和摆动倍率。
- 已接入选中 WeaponPlatform 的已安装配件：
  读取武器 stat 时保留已安装配件，支持 CE 的 stat 替换、偏移和倍率；切换品质/脚架时也不会丢失配件影响。
- 已添加手动配件配置菜单：
  对 WeaponPlatform 可在计算器中临时增删兼容配件，重算武器 stat，不修改游戏内真实武器。
- 已添加特殊弹丸近似提示：
  对瞬发、多弹丸、推进/变速、特殊轨迹、炮击属性和引信/空爆修正显示提示，避免把普通直射模型误读为完全精确。
- 已添加第一版 DPS/伤害拆分：
  直击伤害按当前蒙特卡洛逐发命中率计算期望 DPS；
  基础直击、CE 次级伤害和 RimWorld 额外伤害作为直击伤害合计；
  爆炸/范围伤害作为范围伤害单列，并沿用直击命中率；
  CE 破片作为纸面伤害单列，不乘命中率。
- 已反编译并接入三种 CE 瞄准模式的射击时间差异：
  瞄准射击会先走普通 `warmupTime`，再追加一次距离相关的第二瞄准循环；
  额外瞄准时间为 30-240 tick，按 `distance / 100` 插值；
  快速射击和压制射击只走一次普通预热；
  压制射击在射击时间上等同快速射击，主要差异是允许压制/不可见目标与目标部位限制。
- 持续 DPS 现在会在载入武器后记录弹匣容量、预热、冷却和换弹时间，并按当前距离/瞄准模式动态重算持续射速。
- 已添加 `FasterRepeatShots` 高级选项：
  开启时按 CE `RecalculateWarmupTicks()` 的同方向重复射击近似缩短后续预热；
  快速/瞄准射击默认重复预热约为原来的 25%，压制射击约为 10%，并用后坐力残余做下限修正；
  结果框会提示该估算默认继续向同一方向射击。

## 已移除

- CE 自动目标部位模式。
  - 从当前 TODO 移除，因为它依赖目标护甲/衣物和弹丸穿甲，超出当前手动目标部位模式的目标。
- CE 悬浮窗或选中 pawn gizmo 接入。
  - 从当前 TODO 移除。未来如果需要自动化，应通过“自动读取当前武器、弹药、pawn、目标数据”处理，而不是把悬浮窗当成模型输入来源。
- 实时战斗状态读取。
  - 目标移动、黑暗、天气、烟雾、压制状态、当前瞄准/射击模式等暂不列为目标。
